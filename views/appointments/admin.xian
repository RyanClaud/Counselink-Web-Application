<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>{{ title }} | CounseLink</title>
</head>
<body class="bg-gray-100">
  <div class="flex min-h-screen">
    <!-- Admin Sidebar -->
    <div class="w-64 bg-white shadow-lg">
        <div class="p-4 border-b flex justify-between items-center">
            <a href="/dashboard" class="text-xl font-bold text-indigo-600">CounseLink</a>
        </div>
        <nav class="py-4">
            <a href="/dashboard" class="block px-6 py-3 text-gray-600 hover:bg-gray-100 hover:text-gray-800 {{#if (eq title 'Admin Dashboard')}}bg-gray-200 font-semibold text-gray-800{{/if}}">Dashboard</a>
            <a href="/admin/users" class="block px-6 py-3 text-gray-600 hover:bg-gray-100 hover:text-gray-800 {{#if (eq title 'User Management')}}bg-gray-200 font-semibold text-gray-800{{/if}}">User Management</a>
            <!-- <a href="/admin/reports" class="block px-6 py-3 text-gray-600 hover:bg-gray-100 hover:text-gray-800">Reports</a> -->
        </nav>
        <div class="absolute bottom-0 w-64 p-4 border-t">
             <a href="/logout" class="block text-center bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded text-sm">Logout</a>
        </div>
    </div>

    <!-- Main Content -->
    <main class="flex-1">
        <div class="p-8">
            {{#if success_msg}}
                <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-6" role="alert"><p>{{success_msg}}</p></div>
            {{/if}}
            {{#if error_msg}}
                <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6" role="alert"><p>{{error_msg}}</p></div>
            {{/if}}
            {{{ body }}}
        </div>
    </main>
  </div>
</body>
</html>